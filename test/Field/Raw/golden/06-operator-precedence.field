/**
  * Parsing operator precedence
  */

// Externals
ext a : field;
ext b : field;
ext c : field;
ext d : field;
ext e : field;
ext f : field;
ext g : field;
ext h : field;

assert
	  a * b
	- c * d
    ==
	  e / f
	+ g / h
xor
	  a * b
	- c * d
    >=
	  e / f
	+ g / h
;
