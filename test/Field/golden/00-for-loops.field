for i = 1 to 2 do
    let i' : field = i;
    for j = 2 to 3 do
        let k : field = i * j;
        assert k == i' * j;
    end;
    let p : field = i;
    for l = 1 to 2 do
        let p : field = p * l;
    end;
end;
